# AstrBot 联网搜索插件

这是一个AstrBot插件，提供联网搜索。

## 安装方法

1. 将本插件目录复制到AstrBot的 `data/plugins/` 目录下
2. 确保已安装Python依赖 `pip3 install "httpx>=0.24.0" ddgs -i https://pypi.tuna.tsinghua.edu.cn/simple`[可能需要手动安装]
3. 在AstrBot WebUI中启用插件

## 使用方法

安装即用

## 版本历史

- v2.0.0：修复bug
- v1.0.0：初始版本
